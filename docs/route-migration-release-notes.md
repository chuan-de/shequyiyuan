# 路由迁移版本说明（vNext）

## 本次发布
- 新英文路由保持可用：`/api/v1/auth`、`/api/v1/health`。
- 旧拼音路由过渡兼容：`/api/v1/yonghu`、`/api/v1/jiankang`。

## 旧路由下线计划
- **监控窗口**：2026-05-05 ~ 2026-06-30。
- **冻结日期**：2026-07-01（不再新增旧路由调用方）。
- **目标下线日期**：2026-08-01。
- **替代接口**：
  - `/api/v1/yonghu/*` -> `/api/v1/auth/*`
  - `/api/v1/jiankang` -> `/api/v1/health`

## 下线准入阈值
- 连续 14 天旧路由调用量占比 < 1%。
- 连续 14 天旧路由绝对调用量 < 100 次/天。

## 监控与执行
- 通过 `hospital_api_requests_total{uri=...}` 观察旧路由调用量。
- 达到阈值后，进入下线变更：删除兼容路由、执行全量回归测试并发布。
