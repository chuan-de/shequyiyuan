# 🚫 ARCHIVE ONLY（仅归档目录）

此目录 `backend-rewrite/` 是历史副本。

- **仅归档，不参与构建联调**。
- **禁止作为运行入口**。
- 团队统一后端入口：`server/backend-rewrite/`。

若你需要启动后端，请使用：

```bash
cd server/backend-rewrite && mvn spring-boot:run
```
